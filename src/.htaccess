# Disable Symfony routing for this directory
# Allow direct access to index.php

DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow access to PHP files
<FilesMatch "\.php$">
    Require all granted
</FilesMatch>